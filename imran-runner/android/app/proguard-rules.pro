# Imran Runner keeps no reflective entry points, so the AndroidX/Compose
# consumer rules are sufficient. Line numbers are kept so a stack trace from a
# phone is still readable.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
